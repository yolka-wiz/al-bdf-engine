# HANDOFF — Server Migration 2026-08-08

**Status: development STOPPED. Everything below is the authoritative state
for resuming on the new server.**

## Where things live (all GitHub, yolka-wiz org)

| Repo | Purpose | Source of truth |
|---|---|---|
| `al-bdf-engine` | albdf PDF engine (code, docs, plans, DB) | main + m14 branches |
| `yolka` | Hermes profile export (SOUL, memory, skills, config) | main |

## Repo state (al-bdf-engine)

- **main** = `e667fe0a` (0.3.0 released: GUI + M13 RTL wiring, AppImage
  `albdf-0.3.0-x86_64.AppImage` sha256 `7f6e4204...`, CLI tarball
  `albdf-0.3.0-linux-x86_64.tar.gz` sha256 `5128bf82...`)
- **M14 in progress — TWO WIP branches pushed (resume here):**
  - `m14/form-field-ap` — **task #41**: headless form-fill emits NO AP (worse
    than tofu: `PDFFormManager::drawFormField` is a Q_UNUSED no-op in core).
    RED test scaffolding WIP-committed; GREEN not started. Needs: `--font`
    CLI option (mirror add-text), RTL branch in
    `PDFDocumentBuilder::updateAnnotationAppearanceStreams` using
    `PDFRTLTextEngine::create`.
  - `m14/freetext-ap` — **task #42**: FreeText RTL AP. RED committed
    `071ec87a` (`tst_rtlfreetexttest.cpp` — asserts /Type0 + /FontFile2 in
    AP /Resources). GREEN impl WIP-committed (~116 lines in
    `pdfdocumentbuilder.{cpp,h}`: additive early-return branch for RTL
    FreeText; **NOT yet built/verified** — first action on resume: build +
    run that test, fix, full suite).
- **DB** (`db/albdf.db`, gitignored → pushed explicitly, see below): tasks
  #1–#40 done, **#41/#42 open** (M14). Contains full task history + refs.
- **Plans**: copied into `plans/handoff/` in this commit. Master plan:
  `plans/PLAN.md` (M11 checked, M12/M13 landed).

## Resume checklist (new server)

1. `git clone git@github.com:yolka-wiz/al-bdf-engine.git`
2. `git fetch origin` — get `m14/form-field-ap` + `m14/freetext-ap`
3. Restore DB: `git show origin/handoff-migration:db/albdf.db > db/albdf.db`
   (or copy from this repo — it is committed on the `handoff-migration`
   branch).
4. Branch-protection: ruleset active (PR + 1 approval, owner admin bypass).
   Tokens: fine-grained PATs in `~/.config/gh/hosts.yml` (Contents: write,
   `user` scope) — copy the file (mode 600), NOT into the repo.
5. M14: finish freetext GREEN first (build + test), then form-field RED→GREEN.
6. Toolchain facts: vcpkg at `/home/agent/vcpkg-cache/vcpkg` (NOT
   `/workspace/vcpkg`); system Qt 6.8.2; CI toolchain (ICU 73, Qt 6.8.3)
   in `docs/PROBLEMS.md`; build/test commands in AGENTS.md + this repo's
   CI workflow.

## Credentials / secrets

- **NEVER commit**: `~/.config/gh/hosts.yml`, fine-grained PATs, the admin
  token. The `yolka` repo has `.env.example` only; `config.yaml` there has
  placeholders.
- Profile PATCH needs the `user` scope (already granted on the stored token).

## Profile export (yolka repo)

- Refreshed from `/home/agent/.hermes/profiles/yolka/`:
  - `SOUL.md` (identity), `config.yaml` (Hermes config, secrets redacted),
    `memories/MEMORY.md` + `memories/USER.md`, `skills/` (curated),
    `build-identity.sh` (refresh recipe), `VERSION` (pinned Hermes version).
- New server: `bash build-identity.sh` after installing Hermes at the pinned
  version.

## Open items (do NOT lose)

- M14 tasks #41/#42 (above) — the immediate resume target
- M14 font-source decision: `--font` option on form-fill (mirrors add-text)
- 2 cosmetic probe commits in history (force-push surgery deferred)
- Benchmark threshold tightening (non-gating)
- QFlags 64-bit flags class — verify on Qt 6.10 build (CI Qt 6.8.3
  truncates silently)
- Upstream PDF4QT tracking (repo ~1.4k commits behind, MIT relicensed)
